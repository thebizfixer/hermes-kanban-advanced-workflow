---
plan_id: anchors-fixture
---

# Anchor fixture

Co-located pin (auto-verified): full repo-relative path + L on same line.

**File:** backend/app/services/foo.py

See `backend/app/services/foo.py` L10 for `def load_config`.

Declared pin example (preferred in optimization cards):

```text
Anchor: backend/app/services/foo.py::load_config@L10
```

```python
def load_config():
    pass
```
